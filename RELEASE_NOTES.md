# 릴리즈 노트

## v0.1.3 (2026-09-20)

문서 링크를 여는 수정키 동작을 Chrome 방식에 맞추고, 기존 분할 화면 동작은 별도의 키로 분리했습니다.

### 문서 링크 및 탭 동작

- `Ctrl+클릭`(macOS에서는 `Cmd+클릭`)으로 문서를 현재 화면을 유지한 채 백그라운드 새 탭에서 열 수 있습니다.
- 기존 `Ctrl+클릭`의 분할 화면 동작을 `Alt+클릭`으로 변경했습니다.
- Electron이 생성하는 링크 탭도 `background-tab` 요청을 존중해 활성 탭을 불필요하게 전환하지 않습니다.
- 새 탭 요청은 현재 Outline 페이지가 속한 창으로 안전하게 라우팅하며, 허용된 Outline 출처의 링크만 앱 내부 탭으로 엽니다.

태그: `v0.1.3`

---

## v0.1.2 (2026-09-20)

탭을 자유롭게 정리하고 창 사이로 옮길 수 있는 드래그 기능과 사용자 지정 키보드 단축키를 추가했습니다.

### 탭 드래그 및 다중 창

- 탭을 좌우로 드래그해 순서를 변경할 수 있습니다.
- 탭을 탭 바 밖으로 끌어 새 창으로 분리할 수 있습니다.
- 분리한 탭을 다른 창의 탭 바로 옮길 수 있습니다.
- 탭을 이동할 때 페이지를 다시 로드하지 않으므로 스크롤 위치와 진행 중인 회의 녹음 상태가 유지됩니다.
- 탭 분리 중 현재 이동 위치와 결과를 알아보기 쉽도록 커서를 따라가는 미리보기 UI를 추가했습니다.
- 여러 창과 탭의 소유권, 드래그 상태, IPC 라우팅을 통합 관리하는 창 관리 구조를 도입했습니다.

### 사용자 지정 키보드 단축키

- `Ctrl/Cmd+,` 또는 탭 바의 키보드 버튼으로 단축키 설정 패널을 열 수 있습니다.
- 새 탭, 탭 닫기, 탭 전환, 새 창, 새로 고침, 뒤로/앞으로 이동, 로그인 링크 열기 등 주요 동작의 키 조합을 변경하거나 해제할 수 있습니다.
- `Backspace`로 바인딩을 해제하고 `Esc`로 변경을 취소할 수 있습니다.
- 동일한 키 조합이 여러 동작에 지정되면 충돌 항목을 표시합니다.
- 사용자 설정은 `userData/keybindings.json`에 저장하며, 기본값과 다른 항목만 기록합니다.
- macOS에서는 일반 단축키의 `Mod`를 `Cmd`로 처리하고, OS 앱 전환과 겹치는 탭 순환은 모든 플랫폼에서 `Ctrl+Tab`을 유지합니다.

관련 커밋: `05a98c8`, `9848bd0`

태그: `v0.1.2`

---

## v0.1.1 (2026-08-01)

Outline 자체 서버 코드를 수정하지 않고 사용할 수 있는 Electron 데스크톱 클라이언트의 첫 공개 기능 집합입니다.

### 기본 클라이언트 및 인증

- 자체 호스팅 Outline 서버를 데스크톱 앱에서 로드하는 Electron 클라이언트를 추가했습니다.
- 기본 Outline 주소를 `https://wiki.tukadlab.cloud`로 변경했습니다.
- `outline-electron://` 커스텀 프로토콜을 통한 이메일 로그인 딥링크 처리를 추가했습니다.
- 클립보드에 복사한 Outline 로그인 링크를 앱 내부에서 여는 흐름을 지원합니다.
- 렌더러 샌드박스와 context isolation을 유지하면서 preload를 번들링하도록 빌드 구성을 정리했습니다.

### 회의 녹음 및 실시간 전사

- Outline 편집기에서 `/회의` 명령을 감지해 회의 녹음 패널을 표시합니다.
- 마이크와 시스템 오디오를 함께 캡처하고 자체 호스팅 STT 서버로 실시간 전송합니다.
- faster-whisper `large-v3-turbo` 기반 실시간 한국어 전사와 서버 모델 워밍업을 지원합니다.
- 실시간 부분 전사와 확정 세그먼트를 문서 편집 위치에 자연스럽게 삽입합니다.
- 마이크/시스템 채널을 분리해 화자별로 전사하고 화자 라벨을 표시합니다.
- 녹음 종료 후 화자 라벨을 오프라인으로 다시 정리할 수 있습니다.
- 예상치 못한 WebSocket 연결 종료 시 자동 재연결합니다.
- 전사 삽입 중 편집 화면의 스크롤 위치가 바뀌지 않도록 개선했습니다.
- 회의 패널을 우측 하단에 고정하고 최소화, 녹음 종료 후 자동 닫기 등 패널 동작을 다듬었습니다.

### 탭

- 상단 탭 바와 다중 탭 탐색 기능을 추가했습니다.
- 이 버전에서는 탭 생성, 전환, 닫기를 지원하며 탭 드래그와 창 간 이동은 지원하지 않습니다.

### 패키징 및 플랫폼 지원

- electron-builder 기반 Windows 설치 패키지와 Outline 앱 아이콘을 추가했습니다.
- macOS용 DMG 패키징, 권한 설명, hardened runtime 설정을 추가했습니다.
- Linux용 AppImage 및 DEB 패키징을 추가했습니다.
- DEB 설치 시 Electron의 Linux sandbox 권한 문제를 자동으로 보정하도록 설치 후 스크립트를 추가했습니다.

태그: `v0.1.1`
기준 커밋: `d1e9fa8`
